#include<stdio.h>
int fibonocci(int)
int main()
{
{
	int n,i;
	for (i=1;i<=n;i++)
	printf("%d\t",fibonocci(i));
}
int fibonocci(int n)
{
	if(n==0);
	return 0;
	elseif(n==1)
	return 1;
	else
	return(fibonocci(n-1)+fibonocci(n-2));	
}
return 0;
}
